import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";


const Welcome = () => {
    return (
        <SafeAreaView>
            <Text className={'text-red-500'}>eee</Text>
        </SafeAreaView>
    )
}

export default Welcome;